// function Header({headers, serials = true, actions=true}: {headers: string[], serials?: boolean, actions?: boolean}) {
//     return (  );
// }

// function Body() {

// }

// export default TableHeader;