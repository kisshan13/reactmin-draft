import { memo, useEffect } from "react";
import { useResource } from "./Resource";
import { useReactminStore } from "../react-min-store";
import type { IResourceType, ReactminResource } from "../types/types";

export default function ResourceType(props: IResourceType) {
  return <></>;
}
